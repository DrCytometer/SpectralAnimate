% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unmixing_matrix_animate.R
\name{unmixing.matrix.animate}
\alias{unmixing.matrix.animate}
\title{Unmixing Matrix Animation}
\usage{
unmixing.matrix.animate(
  spectra,
  color.palette = "viridis",
  title = NULL,
  figure.dir = ".",
  animation.filename = "unmixing_matrix_transition.gif",
  show.legend = FALSE,
  plot.width = NULL,
  plot.height = NULL,
  nframes = 80,
  fps = 15,
  transition.length = 3,
  state.length = 2,
  res = 150,
  save = TRUE
)
}
\arguments{
\item{spectra}{Normalised spectral signatures (mixing matrix). Fluorophores
in rows, detectors in columns. Both row and column names are required.}

\item{color.palette}{Viridis palette name for the heatmap fill. Default is
\code{"viridis"}. Options: \code{"magma"}, \code{"inferno"}, \code{"plasma"}, \code{"viridis"},
\code{"cividis"}, \code{"rocket"}, \code{"mako"}, \code{"turbo"}.}

\item{title}{Optional character string prepended to the animation filename.
Default is \code{NULL}.}

\item{figure.dir}{Output directory for the saved GIF. Default is \code{"."}.}

\item{animation.filename}{Base filename for the GIF output. Default is
\code{"unmixing_matrix_transition.gif"}.}

\item{show.legend}{Logical. Whether to show the fill legend. Default is
\code{FALSE}.}

\item{plot.width}{Width of each frame in inches. Default is \code{NULL}
(auto-scaled by number of detectors).}

\item{plot.height}{Height of each frame in inches. Default is \code{NULL}
(auto-scaled by number of fluorophores).}

\item{nframes}{Total number of animation frames. Default is \code{80}.}

\item{fps}{Frames per second for the output GIF. Default is \code{15}.}

\item{transition.length}{Relative length of the morphing transition between
states (passed to \code{transition_states()}). Default is \code{3}.}

\item{state.length}{Relative length of the pause on each state (passed to
\code{transition_states()}). Default is \code{2}.}

\item{res}{Resolution in ppi for the rendered frames. Default is \code{150}.}

\item{save}{Logical. If \code{TRUE}, saves the GIF to \code{figure.dir}. If \code{FALSE},
returns the rendered animation object invisibly. Default is \code{TRUE}.}
}
\value{
The rendered \code{gganimate} animation object, invisibly.
}
\description{
Animates the transition from the spectral spillover (mixing) matrix to the
unmixing matrix (Moore-Penrose pseudoinverse via OLS). Both matrices are
displayed in a consistent fluorophore x detector orientation — the unmixing
matrix is transposed before display so axes remain stable across the
transition.

Each matrix uses its own independent colour scale so that the dynamic range
of peaks and troughs is fully visible in both the mixing and unmixing
heatmaps. This is achieved by rescaling each matrix to [0, 1] before
combining into a single animated \code{ggplot}, then using a shared
\code{scale_fill_viridis_c} with \code{limits = c(0, 1)}.

The mixing matrix (normalised spectral signatures) has fluorophores as rows
and detectors as columns. The pseudoinverse returned by \code{unmix.ols()} has the
opposite orientation (detectors x fluorophores); it is transposed internally
so that both heatmaps share the same axis layout.
}
