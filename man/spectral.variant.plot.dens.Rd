% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_variant_density_plot.R
\name{spectral.variant.plot.dens}
\alias{spectral.variant.plot.dens}
\title{Spectral Variant Density Plot}
\usage{
spectral.variant.plot.dens(
  spectra.variants,
  median.spectrum,
  title = "Spectral_variants_density",
  save = FALSE,
  plot.width = NULL,
  plot.height = 5,
  plot.dir = "./figure_spectral_variants",
  variant.color = "red",
  variant.alpha = 0.05,
  variant.linewidth = 1.5,
  median.line.color = "black",
  median.linewidth = 1
)
}
\arguments{
\item{spectra.variants}{Matrix of spectral variants; rows are variants,
columns are detectors. Column names are used as x-axis labels.}

\item{median.spectrum}{Numeric vector of the median signal intensity across
detectors. Must have the same length as \code{ncol(spectra.variants)}.}

\item{title}{Plot title and output filename prefix. Default is
\code{"Spectral_variants_density"}.}

\item{save}{Logical. If \code{TRUE}, saves the plot to \code{plot.dir}. Default is
\code{FALSE}.}

\item{plot.width}{Width of the saved plot in inches. Default is \code{NULL},
which auto-scales based on the number of detectors.}

\item{plot.height}{Height of the saved plot in inches. Default is \code{5}.}

\item{plot.dir}{Output directory for the saved plot. Default is
\code{"./figure_spectral_variants"}.}

\item{variant.color}{Line color for the individual variant traces. Default
is \code{"red"}.}

\item{variant.alpha}{Alpha transparency for the variant lines. Default is
\code{0.05}.}

\item{variant.linewidth}{Thickness of the variant lines. Default is \code{1.5}.}

\item{median.line.color}{Color of the median reference line. Default is
\code{"black"}.}

\item{median.linewidth}{Thickness of the median reference line. Default is
\code{1}.}
}
\value{
The \code{ggplot} object representing the variant density plot. When
\code{save = TRUE}, a JPEG is also written to \code{plot.dir}.
}
\description{
Creates a static visualization showing multiple spectral variants overlaid
against a median spectral signature to illustrate consistency and variance.
Each variant is plotted as a semi-transparent line, and the median spectrum
is drawn on top as a solid reference trace.
}
