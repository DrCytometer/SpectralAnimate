% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_heatmap_animate.R
\name{spectral.heatmap.animate}
\alias{spectral.heatmap.animate}
\title{Animated Spectral Heatmap}
\usage{
spectral.heatmap.animate(
  spectra,
  title = NULL,
  plot.dir = NULL,
  legend.label = "Intensity",
  color.palette = "viridis",
  show.legend = FALSE,
  plot.width = NULL,
  plot.height = NULL,
  fps = 2,
  pause.seconds = 2,
  save = TRUE
)
}
\arguments{
\item{spectra}{Matrix or dataframe containing spectral data;
format: fluorophores x detectors.}

\item{title}{Optional prefix for the output animation filename.}

\item{plot.dir}{Optional output directory. Default is \code{NULL}, in which
case \code{getwd()} is used.}

\item{legend.label}{Character string that will appear on the heatmap legend.
Default is \code{"Intensity"}.}

\item{color.palette}{Optional character string defining the viridis color
palette. Default is \code{"viridis"}. Options: \code{"magma"},
\code{"inferno"}, \code{"plasma"}, \code{"viridis"}, \code{"cividis"},
\code{"rocket"}, \code{"mako"}, \code{"turbo"}.}

\item{show.legend}{Logical. If \code{TRUE}, the fill legend is included.
Default is \code{TRUE}.}

\item{plot.width}{Width of the output animation in pixels. Default is
\code{NULL}, which auto-scales based on the number of detectors.}

\item{plot.height}{Height of the output animation in pixels. Default is
\code{NULL}, which auto-scales based on the number of fluorophores.}

\item{fps}{Frames per second for the output GIF. Default is \code{10}.}

\item{pause.seconds}{How long (in seconds) to hold on the completed heatmap
at the end of the animation. Default is \code{2}.}

\item{save}{Logical. If \code{TRUE} (default), saves a GIF to
\code{plot.dir}. If \code{FALSE}, returns the \code{gganimate} object
invisibly for further use.}
}
\value{
If \code{save = TRUE}, writes a GIF and returns the file path
invisibly. If \code{save = FALSE}, returns the animated plot object.
}
\description{
Animates the spectral matrix heatmap using \code{gganimate}, revealing one row of
spectra (fluorophore) at a time from bottom to top, then pausing on the
completed heatmap. Saves the result as a GIF file.
}
