% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_variant_animate.R
\name{spectral.variant.animate}
\alias{spectral.variant.animate}
\title{Spectral Variant Animation}
\usage{
spectral.variant.animate(
  spectra.variants,
  title = "Spectral_Variant_Animation",
  save = TRUE,
  plot.dir = "./figure_spectral_variants",
  variant.fill.color = "red",
  variant.fill.alpha = 0.3,
  variant.linewidth = 1,
  plot.width = 16,
  plot.height = 9
)
}
\arguments{
\item{spectra.variants}{Matrix of spectral variants; rows are variants,
columns are detectors. Row names are optional; column names are used as
x-axis labels.}

\item{title}{Filename prefix for the output GIF. Default is
\code{"Spectral_Variant_Animation"}.}

\item{save}{Logical. If \code{TRUE} (default), saves the GIF to \code{plot.dir}.}

\item{plot.dir}{Output directory for the saved GIF. Default is
\code{"./figure_spectral_variants"}.}

\item{variant.fill.color}{Fill color for the variability ribbon shown at the
end of the animation. Default is \code{"red"}.}

\item{variant.fill.alpha}{Alpha transparency for the variability ribbon.
Default is \code{0.3}.}

\item{variant.linewidth}{Line thickness for the individual variant traces.
Default is \code{1}.}

\item{plot.width}{Width of the output animation in inches. Default is \code{16}.}

\item{plot.height}{Height of the output animation in inches. Default is \code{9}.}
}
\value{
The rendered \code{gganimate} animation object, invisibly. When
\code{save = TRUE}, the GIF is also written to \code{plot.dir}.
}
\description{
Visualizes the variability of spectral signatures by animating individual
variant lines sequentially, followed by a dynamic ribbon representing the
full range of variability across all variants. The first few variants appear
slowly (one every ten frames) to allow inspection; subsequent variants are
added rapidly (one per frame). A shaded ribbon summarising the min/max range
across all variants is then revealed at the end.
}
