% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_ribbon_animate.R
\name{spectral.ribbon.animate}
\alias{spectral.ribbon.animate}
\title{Spectral Ribbon Animation}
\usage{
spectral.ribbon.animate(
  ribbon.plot.data,
  asp,
  title = NULL,
  figure.dir = NULL,
  save = TRUE,
  color.palette = "rainbow",
  plot.width = 15,
  plot.height = 10,
  plot.filename = "spectral_ribbon_plot.jpg",
  animation.filename = "spectral_animation.gif"
)
}
\arguments{
\item{ribbon.plot.data}{A matrix or dataframe containing the raw spectral
data. Rows are events, columns are detectors.}

\item{asp}{The AutoSpectral parameter list. Used for biexponential
transformation parameters, axis breaks, limits, and plot styling.}

\item{title}{An optional character string prepended to both the plot and
animation filenames. Default is \code{NULL}.}

\item{figure.dir}{Output directory for the saved files. If \code{NULL} (default),
\code{asp$figure.spectral.ribbon.dir} is used.}

\item{save}{Logical. If \code{TRUE} (default), the plot and animation are saved to
\code{figure.dir}. If \code{FALSE}, the rendered animation is returned invisibly
without saving.}

\item{color.palette}{Character string defining the color palette. Default is
\code{"rainbow"}, mimicking a FlowJo colour scheme. Viridis options are also
accepted: \code{"magma"}, \code{"inferno"}, \code{"plasma"}, \code{"viridis"}, \code{"cividis"},
\code{"rocket"}, \code{"mako"}, \code{"turbo"}.}

\item{plot.width}{Width of the saved plot in inches. Default is \code{15}.}

\item{plot.height}{Height of the saved plot in inches. Default is \code{10}.}

\item{plot.filename}{Base filename for the static JPEG. Default is
\code{"spectral_ribbon_plot.jpg"}.}

\item{animation.filename}{Base filename for the GIF. Default is
\code{"spectral_animation.gif"}.}
}
\value{
The rendered \code{gganimate} animation object, invisibly. When
\code{save = TRUE}, the GIF and a static JPEG are also written to \code{figure.dir}.
}
\description{
Generates an animated spectral ribbon plot that builds the density heatmap
detector by detector using \code{gganimate}. Each frame adds the next detector
column cumulatively, giving a left-to-right reveal of the full ribbon. An
optional static JPEG is also saved alongside the GIF.

The function is called internally by \code{clean.controls()}. To use it directly,
supply data via \code{ribbon.plot.data} (a matrix or data.frame, events x
detectors) and a valid \code{asp} parameter list.
}
