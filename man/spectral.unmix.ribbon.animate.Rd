% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_unmix_ribbon_animate.R
\name{spectral.unmix.ribbon.animate}
\alias{spectral.unmix.ribbon.animate}
\title{Spectral Unmixing Ribbon Animation}
\usage{
spectral.unmix.ribbon.animate(
  data.list,
  spectra,
  spectral.channel,
  asp,
  fluor.colors = NULL,
  max.points = 500,
  color.palette = "viridis",
  ribbon.color.palette = "rainbow",
  title = NULL,
  figure.dir = ".",
  animation.filename = "spectral_unmix_ribbon.gif",
  plot.width = 16,
  plot.height = 9,
  nframes = 120,
  fps = 100,
  res = 150,
  transition.length = 2,
  state.length = 3,
  save = TRUE
)
}
\arguments{
\item{data.list}{A \strong{named} list of matrices or data.frames. Each element
is a single-colour control: rows are events, columns are spectral detectors.
Names are used as fluorophore labels. Column names must match
\code{spectral.channel}.}

\item{spectra}{Normalised spectral signatures (mixing matrix), fluorophores x
detectors. Row names must match \code{names(data.list)}, column names must match
\code{spectral.channel}.}

\item{spectral.channel}{Character vector of spectral detector names (the raw
x-axis). Must match column names in each element of \code{data.list} and in
\code{spectra}.}

\item{asp}{The AutoSpectral parameter list. Used for biexponential transform,
axis breaks, limits, and palette parameters.}

\item{fluor.colors}{Optional named character vector mapping fluorophore names
to colours. If \code{NULL} (default), colours are drawn automatically from
\code{scales::hue_pal()}.}

\item{max.points}{Maximum number of events to sample \strong{per element of
\code{data.list}} for the scatter plot panels. Default is \code{500}. Sampling is
applied independently to each fluorophore's data to keep rendering fast.}

\item{color.palette}{Viridis palette name for the central heatmap fill.
Default is \code{"viridis"}.}

\item{ribbon.color.palette}{Palette for the raw density ribbon fill. Default
is \code{"rainbow"}, matching \code{spectral.ribbon.plot()}. Set to a viridis name to
override.}

\item{title}{Optional character string prepended to the animation filename.}

\item{figure.dir}{Output directory for the saved GIF. Default is \code{"."}.}

\item{animation.filename}{Base filename for the GIF. Default is
\code{"spectral_unmix_ribbon.gif"}.}

\item{plot.width}{Width of the combined figure in inches. Default is \code{16}.}

\item{plot.height}{Height of the combined figure in inches. Default is \code{9}.}

\item{nframes}{Total animation frames. Default is \code{120}.}

\item{fps}{Frames per second. Default is \code{15}.}

\item{res}{Resolution in ppi. Default is \code{150}.}

\item{transition.length}{Relative transition length per state pair. Default
is \code{2}.}

\item{state.length}{Relative pause length per state. Default is \code{3}.}

\item{save}{Logical. Save GIF if \code{TRUE}. Default is \code{TRUE}.}
}
\value{
Invisibly returns a list with elements \code{raw.plot}, \code{heatmap.plot},
\code{right.plot}, and \code{animation} (the rendered \code{magick} GIF object).
}
\description{
Produces a three-panel animation illustrating spectral unmixing of flow
cytometry data. The layout is:

Raw spectral scatter | Unmixing matrix heatmap | Unmixed scatter

All fluorophores are animated simultaneously through three global states:
\enumerate{
\item \strong{Raw} -- all events shown at their raw spectral positions on
the left panel; right panel is empty.
\item \strong{Residual} -- left panel points smoothly transition to their
residual positions (\verb{residuals = raw - unmixed \\\%*\\\% spectra}); unmixed
data appears simultaneously on the right panel.
\item \strong{Pause} -- final state held so the viewer can compare left and
right panels.
}

Because all fluorophores share the same three state labels, both animated
panels always produce an identical frame count, avoiding stitching
mismatches. Panels are stitched per-frame using \code{magick}.
}
\section{Animation states}{

\describe{
\item{\code{"1_raw"}}{Raw spectral scatter (left only).}
\item{\code{"2_residual"}}{Left transitions raw->residual; right fades in unmixed.}
\item{\code{"3_pause"}}{Hold final state.}
}
}

